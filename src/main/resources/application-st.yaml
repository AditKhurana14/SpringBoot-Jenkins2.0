spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}

app:
  environment: ST
  input-folder: D:/batch/input
  processed-folder: D:/batch/processed
  error-folder: D:/batch/error